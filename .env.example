# Configuração do Banco de Dados MongoDB
# Substitua por sua string de conexão do MongoDB
MONGO_URI=mongodb://localhost:27017/orkut

# Chave secreta para JWT (use uma chave forte e única)
# Você pode gerar uma usando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=sua_chave_secreta_jwt_muito_forte_aqui

# Porta do servidor (opcional, padrão: 5000)
PORT=5000

# Ambiente (development, production, test)
NODE_ENV=development 